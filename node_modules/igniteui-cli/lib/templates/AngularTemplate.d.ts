export declare class AngularTemplate implements Template {
    private rootPath;
    components: string[];
    controlGroup: string;
    listInComponentTemplates: boolean;
    listInCustomTemplates: boolean;
    id: string;
    name: string;
    description: string;
    dependencies: Array<string | object>;
    framework: string;
    projectType: string;
    hasExtraConfiguration: boolean;
    packages: any[];
    protected widget: string;
    /**
     * Creates a new AngularTemplate for a root path (pass in __dirname)
     */
    constructor(rootPath: string);
    generateFiles(projectPath: string, name: string, options: {}): Promise<boolean>;
    registerInProject(projectPath: string, name: string, options?: {
        [key: string]: any;
    }): void;
    getExtraConfiguration(): ControlExtraConfiguration[];
    setExtraConfiguration(extraConfigKeys: {}): void;
    protected getBaseVariables(name: string): {};
    protected ensureSourceFiles(): void;
    protected folderName(name: string): string;
    protected fileName(name: string): string;
}
